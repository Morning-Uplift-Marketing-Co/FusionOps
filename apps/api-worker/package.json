{
  "name": "lp-factory-api",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 execute fusionops-main --local --file=./migrations/0001_init.sql",
    "migrate:remote": "wrangler d1 execute fusionops-main --remote --file=./migrations/0001_init.sql",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.0.6",
    "@neondatabase/serverless": "^1.0.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250124.0",
    "wrangler": "^4.67.0"
  }
}
