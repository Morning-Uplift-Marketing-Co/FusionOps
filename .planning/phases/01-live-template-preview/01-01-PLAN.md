---
phase: 01-live-template-preview
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/Wizard/PreviewModal.test.jsx
  - src/hooks/usePreviewDebounce.test.js
  - src/components/DiffViewer.test.jsx
  - src/utils/html-diff.test.js
autonomous: true
requirements: [PREV-01, PREV-02, PREV-03, PREV-04]
---

<objective>
Establish test suite for Phase 1 preview components and hooks. Create 4 test files that define acceptance criteria for PreviewModal, usePreviewDebounce, DiffViewer, and html-diff utilities.

Purpose: Test-first approach ensures components are built to spec before implementation. Tests serve as live documentation of expected behavior.

Output: 4 passing test files with 20+ test cases covering all PREV-* requirements.
</objective>

<execution_context>
@/c/Users/Barbara/.claude/get-shit-done/workflows/execute-plan.md

Tests use Vitest 4.0.18 (already installed). Use `npm test -- <file>` to run individual test file.
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-live-template-preview/01-RESEARCH.md

## Key References

**buildPreviewHtml():** src/utils/template-preview-runtime.js (existing, 385 lines)
- Input: templateFiles object + config object (brand, color, copy, etc.)
- Output: HTML string with injected CSS variables
- Already tested and proven in v1.0

**AntiFingerprint.transform():** src/services/AntiFingerprint.js (existing, 1,140 lines)
- Input: HTML string + CSS string + siteId
- Output: Promise<{html: string, summary: {...}}>
- Deterministic per siteId; 62 tests passing

**diff-match-patch:** Library to install via npm
- Used for generating diffs between pre/post fingerprint HTML
- API: `new diff_match_patch().diff_main(str1, str2)` returns array of [op_type, text] tuples

</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PreviewModal.test.jsx test suite</name>
  <files>src/components/Wizard/PreviewModal.test.jsx</files>
  <action>
Create unit test file for PreviewModal component. Test suite covers:

1. **Component Mounting & Rendering** (PREV-01)
   - Modal renders when isOpen=true
   - Modal does not render when isOpen=false
   - iframe element present with sandbox attribute
   - Control buttons present (viewport toggle, fingerprint toggle)

2. **Viewport Toggle** (PREV-02)
   - Default viewport is 'desktop' (1024px width)
   - Clicking viewport toggle changes to 'mobile' (320px width)
   - Clicking again changes back to 'desktop'
   - iframe style.width reflects current viewport (verify CSS)
   - No re-rendering of template HTML on viewport change (verify srcdoc stable)

3. **Fingerprint Toggle** (PREV-04)
   - Default shows previewHtml (original)
   - Clicking fingerprint toggle shows fingerprintedHtml
   - Clicking again shows previewHtml
   - srcdoc attribute reflects current selection

4. **Error States**
   - If previewHtml is empty, show placeholder
   - If generation fails, show error message in iframe
   - Error message includes helpful context

5. **Props & State Management**
   - config prop accepted (object with brand, color, copy, etc.)
   - templateId prop accepted
   - onClose callback triggered when close button clicked

Test framework: Vitest + React Test Library (@testing-library/react)
Assertions: Use expect() from vitest; render() from @testing-library/react
Mock data: Create minimal config object {brand: "Test", primaryColor: "#000", ...}

File structure:
- Import PreviewModal from component path
- Setup: describe('PreviewModal component', () => { ... })
- Min 10 test cases; each <30 lines
- Use beforeEach for common setup (render with mock props)
  </action>
  <verify>
    <automated>npm test -- src/components/Wizard/PreviewModal.test.jsx 2>&1 | grep -E "PASS|FAIL|passed|failed"</automated>
  </verify>
  <done>Test file exists, all tests pass (10+ test cases), covers PREV-01, PREV-02, PREV-04 requirements</done>
</task>

<task type="auto">
  <name>Task 2: Create usePreviewDebounce.test.js test suite</name>
  <files>src/hooks/usePreviewDebounce.test.js</files>
  <action>
Create unit test file for usePreviewDebounce custom hook. Test suite covers:

1. **Debounce Timing** (PREV-03)
   - Hook delays preview refresh by specified delay (default 400ms)
   - Multiple rapid config changes only trigger one preview generation after delay expires
   - First change triggers timer; subsequent changes reset timer
   - Verify timing: change at t=0, change at t=100, change at t=200 → generation at t=600 (400ms after last change)

2. **Real-Time Refresh** (PREV-03)
   - Hook returns previewHtml state
   - previewHtml updates after debounce delay expires
   - Hook returns error state if generation fails
   - Loading state available during debounce

3. **Cleanup on Unmount**
   - Timer is cleared when hook unmounts (no memory leak)
   - AbortController signal aborted on unmount or new timer
   - Cancelled requests ignored (no state update after unmount)

4. **External Fetch Scenario**
   - If hook calls async getTemplateFiles(templateId), AbortController cancels on rapid re-trigger
   - Test with fake async fetch that can be cancelled
   - Verify: if user changes config 3 times in 100ms, only 1 request made (last one)

5. **Dependency Updates**
   - Changing config triggers new timer
   - Changing templateId triggers new timer
   - Changing delay prop triggers new timer
   - Same config/templateId/delay → timer not reset (no unnecessary re-generation)

Test framework: Vitest + @testing-library/react (useHook testing via renderHook)
Assertions: waitFor() for async operations; vi.useFakeTimers() for timing control
Mock data: config = {brand: "Test", ...}; templateId = "tpl-123"

File structure:
- Import usePreviewDebounce from hook path
- Setup: describe('usePreviewDebounce hook', () => { ... })
- Use vi.useFakeTimers() in beforeEach
- Use vi.runAllTimers() or vi.advanceTimersByTime() to simulate delay
- Min 8 test cases; each <40 lines
  </action>
  <verify>
    <automated>npm test -- src/hooks/usePreviewDebounce.test.js 2>&1 | grep -E "PASS|FAIL|passed|failed"</automated>
  </verify>
  <done>Test file exists, all tests pass (8+ test cases), covers PREV-03 requirement with timing verification</done>
</task>

<task type="auto">
  <name>Task 3: Create DiffViewer.test.jsx test suite</name>
  <files>src/components/DiffViewer.test.jsx</files>
  <action>
Create unit test file for DiffViewer component. Test suite covers:

1. **Diff Rendering** (PREV-04)
   - Component accepts preHtml and postHtml as props
   - Component accepts diffs array (output from diff-match-patch)
   - Renders side-by-side or tabbed layout (choose one pattern)
   - Both pre and post HTML visible in output

2. **Visual Diff Highlighting** (PREV-04)
   - Added lines/text highlighted in green
   - Removed lines/text highlighted in red
   - Unchanged text in default color
   - Test with simple HTML diff: <div>old</div> → <div class="fp-123">old</div>
   - Verify: class="fp-123" highlighted as addition

3. **Empty State**
   - If preHtml === postHtml (no changes), show message "No changes"
   - If diffs array is empty, show same message

4. **Large Diff Handling**
   - If diff is >100 lines, truncate to first 50 + count of remaining
   - Show: "... 50 more changes" message
   - Prevents performance issues with very large templates

5. **Props Validation**
   - preHtml prop required (string)
   - postHtml prop required (string)
   - diffs prop optional (defaults to empty array)
   - layout prop optional (defaults to 'side-by-side')

Test framework: Vitest + React Test Library
Mock data:
  - preHtml = '<div>Test</div>'
  - postHtml = '<div class="fp-abc">Test</div>'
  - diffs = [[0, '<div'], [1, ' class="fp-abc"'], [0, '>Test</div>']] (from diff-match-patch format)

File structure:
- Import DiffViewer from component path
- Setup: describe('DiffViewer component', () => { ... })
- Min 6 test cases; each <40 lines
- Use screen.getByText(), container.innerHTML checks for rendering verification
  </action>
  <verify>
    <automated>npm test -- src/components/DiffViewer.test.jsx 2>&1 | grep -E "PASS|FAIL|passed|failed"</automated>
  </verify>
  <done>Test file exists, all tests pass (6+ test cases), covers PREV-04 requirement for diff visualization</done>
</task>

<task type="auto">
  <name>Task 4: Create html-diff.test.js utility test suite</name>
  <files>src/utils/html-diff.test.js</files>
  <action>
Create unit test file for html-diff utility function. This is a wrapper/helper around diff-match-patch for HTML-specific scenarios.

Utility function should expose:
```javascript
export function generateHtmlDiff(preHtml, postHtml, options = {}) {
  // Uses diff-match-patch to compute diffs
  // Returns { diffs, summary: {added: N, removed: N, unchanged: N} }
}
```

Test suite covers:

1. **Basic HTML Diff** (PREV-04)
   - Input: preHtml = '<div>test</div>', postHtml = '<div class="fp">test</div>'
   - Output: diffs array showing class addition
   - summary.added = number of added characters
   - summary.removed = 0
   - summary.unchanged = number of unchanged characters

2. **CSS Class Changes** (PREV-04)
   - Fingerprinting changes class names: btn-primary → btn-fp-xyz
   - Diff detects class name change as removal + addition
   - Test multiple classes: 'btn primary large' → 'btn fp-abc fp-def'

3. **ID Randomization** (PREV-04)
   - Fingerprinting changes IDs: id="form-1" → id="form-fp-abc"
   - Diff detects ID change
   - Summary reflects character count changes

4. **Data Attribute Changes** (PREV-04)
   - data-tracking="voluum" → data-tracking="voluum" (unchanged)
   - data-fp="xyz" added
   - Diff detects new attributes

5. **Whitespace Handling**
   - Diffs handle HTML formatting (newlines, indentation)
   - cleanup option trims excess whitespace
   - Two identical HTML with different formatting → minimal diff

6. **Performance**
   - generateHtmlDiff() with 50KB HTML completes in <100ms
   - No stack overflow on very large HTML (>1MB)

Test framework: Vitest
Mock data: Use real HTML snippets from templates (Astro, Vite/React, static HTML)
Fixtures: Create sample pre/post HTML in test file or as separate .html files

File structure:
- Import generateHtmlDiff from utility path
- Setup: describe('html-diff utility', () => { ... })
- Min 8 test cases; each <30 lines
- Use expect() assertions on diffs and summary properties
  </action>
  <verify>
    <automated>npm test -- src/utils/html-diff.test.js 2>&1 | grep -E "PASS|FAIL|passed|failed"</automated>
  </verify>
  <done>Test file exists, all tests pass (8+ test cases), covers HTML-specific diff scenarios for PREV-04</done>
</task>

</tasks>

<verification>
Run full test suite after all 4 test files created:

```bash
npm test -- src/components/Wizard/PreviewModal.test.jsx \
           src/hooks/usePreviewDebounce.test.js \
           src/components/DiffViewer.test.jsx \
           src/utils/html-diff.test.js
```

All tests must PASS. No failures or skipped tests.

Coverage check:
```bash
npm test -- --coverage src/components/Wizard/PreviewModal.test.jsx \
                       src/hooks/usePreviewDebounce.test.js \
                       src/components/DiffViewer.test.jsx \
                       src/utils/html-diff.test.js
```

Target: 80%+ statement coverage per file.
</verification>

<success_criteria>
- [ ] PreviewModal.test.jsx exists with 10+ passing tests (viewport toggle, fingerprint toggle, error states)
- [ ] usePreviewDebounce.test.js exists with 8+ passing tests (debounce timing, cleanup, dependency updates)
- [ ] DiffViewer.test.jsx exists with 6+ passing tests (diff rendering, highlighting, empty state)
- [ ] html-diff.test.js exists with 8+ passing tests (CSS changes, ID changes, whitespace)
- [ ] All 32+ tests pass without failures
- [ ] 80%+ statement coverage on test files
- [ ] Tests are executable via `npm test` command
- [ ] No console errors or warnings during test execution
- [ ] Tests ready for implementation: all test names clear and actionable
</success_criteria>

<output>
After completion, create `.planning/phases/01-live-template-preview/01-01-SUMMARY.md` with:
- Test count per file (10+8+6+8 = 32+)
- Coverage percentages
- Test execution time
- Key test scenarios covered (PREV-01 through PREV-04)
- Ready status: "Wave 0 test suite complete; ready for Wave 1 component implementation"
</output>
