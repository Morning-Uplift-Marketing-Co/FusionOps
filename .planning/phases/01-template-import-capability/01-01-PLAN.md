---
phase: 01-template-import-capability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/env-preprocessor.js
  - src/utils/html-expression-replacer.js
  - src/utils/__tests__/env-preprocessor.test.js
  - src/utils/__tests__/html-expression-replacer.test.js
autonomous: true
requirements:
  - IMPORT-01
  - IMPORT-02
must_haves:
  truths:
    - "Imported Astro templates receive actual PUBLIC_* env var values at build time (no placeholder expressions in final HTML)"
    - "Post-build HTML contains actual brand name, conversion IDs, and tracking domains (not import.meta.env references)"
    - "Quality checks can detect and report any remaining leaked Astro expressions before deploy"
  artifacts:
    - path: src/utils/env-preprocessor.js
      provides: "Pre-build .astro file transformation to inject env values"
      exports: [preprocessAstroEnvVars]
      min_lines: 50
    - path: src/utils/html-expression-replacer.js
      provides: "Post-build HTML scanning and leaked expression replacement"
      exports: [replaceLeakedExpressions]
      min_lines: 40
    - path: src/utils/__tests__/env-preprocessor.test.js
      provides: "Test coverage for env preprocessing including edge cases"
      min_lines: 60
    - path: src/utils/__tests__/html-expression-replacer.test.js
      provides: "Test coverage for post-build HTML rewriting"
      min_lines: 60
  key_links:
    - from: BuildAdapter (Phase 2)
      to: src/utils/env-preprocessor.js
      via: "Call preprocessAstroEnvVars before astro build"
      pattern: "preprocessAstroEnvVars\\(files,"
    - from: DeployPipeline (Phase 2)
      to: src/utils/html-expression-replacer.js
      via: "Call replaceLeakedExpressions after build completes"
      pattern: "replaceLeakedExpressions\\(html,"
---

<objective>
Fix critical bug where Astro `PUBLIC_*` environment variables are not injected at build time, causing deployed pages to show placeholder expressions instead of customized values.

Purpose: Foundation blocker preventing any deployed site from showing correct brand/conversion data. Must work for all Astro templates before build orchestration can proceed.

Output: Two utility functions (env preprocessor + HTML rewriter) with test coverage demonstrating env value injection works end-to-end.
</objective>

<execution_context>
@.planning/phases/01-template-import-capability/01-RESEARCH.md
@.planning/codebase/STRUCTURE.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

Key existing files referenced:
- `src/utils/template-analyzer.js` — Framework detection patterns (reuse for file type checking)
- `utils/astro-generator.jsx` — Where .env files are currently created (context for env structure)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create env-preprocessor.js with test suite</name>
  <files>
    src/utils/env-preprocessor.js
    src/utils/__tests__/env-preprocessor.test.js
  </files>
  <read_first>
    src/utils/template-analyzer.js (to understand file iteration patterns)
    utils/astro-generator.jsx (to see current .env structure)
  </read_first>
  <action>
Create `src/utils/env-preprocessor.js` with function `preprocessAstroEnvVars(files, envVars)` that:
1. Iterates over files object
2. For each .astro file:
   - Replace all `import.meta.env.PUBLIC_VARNAME || 'fallback'` with the actual value
   - Replace all `import.meta.env.PUBLIC_VARNAME` (no fallback) with the actual value
   - Handle both quoted strings ('...' and "..." and backticks)
   - Escape special chars in values (backslashes, quotes, newlines)
3. Return new files object with replacements applied
4. Do NOT modify non-.astro files

Create test file `src/utils/__tests__/env-preprocessor.test.js` with vitest:
- Test 1: Handles `import.meta.env.PUBLIC_BRAND || "default"` → replaces with actual value
- Test 2: Handles `import.meta.env.PUBLIC_BRAND` (no fallback) → replaces with actual
- Test 3: Escapes special characters in values (quotes, backslashes)
- Test 4: Handles multiple replacements in same file
- Test 5: Does not modify non-.astro files
- Test 6: Returns empty string if env var not in envVars object

Use the regex patterns from 01-RESEARCH.md under "Example 1: Env Var Preprocessing". Return files object with same keys, modified content.
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/env-preprocessor.test.js --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - All 6 test cases pass (grep for "PASS" in vitest output)
    - File `src/utils/env-preprocessor.js` exports `preprocessAstroEnvVars` function
    - Function signature matches: `(files, envVars) => files`
    - No Astro expression leaks remain in output of test cases (grep for "import.meta.env" in test assertions should show 0 matches in replaced output)
  </acceptance_criteria>
  <done>env-preprocessor.js and tests complete; all 6 tests green</done>
</task>

<task type="auto">
  <name>Task 2: Create html-expression-replacer.js with test suite</name>
  <files>
    src/utils/html-expression-replacer.js
    src/utils/__tests__/html-expression-replacer.test.js
  </files>
  <read_first>
    src/utils/env-preprocessor.js (to understand string escaping approach)
  </read_first>
  <action>
Create `src/utils/html-expression-replacer.js` with function `replaceLeakedExpressions(html, envVars)` that:
1. Scan HTML string for any remaining Astro expressions (catch-all for post-build cleanup)
2. Replace patterns:
   - `import.meta.env.PUBLIC_VARNAME || 'fallback'` → actual value
   - `import.meta.env.PUBLIC_VARNAME` → actual value
   - `${import.meta.env.PUBLIC_VARNAME}` (template literal) → just the value
   - Within script tags, style tags, or any HTML content
3. Escape values same as env-preprocessor (quotes, backslashes)
4. Return modified HTML string

Create test file `src/utils/__tests__/html-expression-replacer.test.js` with vitest:
- Test 1: Replaces `import.meta.env.PUBLIC_SITE_NAME || "default"` in HTML
- Test 2: Replaces `${import.meta.env.PUBLIC_CONVERSION_ID}` in template literals (script content)
- Test 3: Handles multiple expressions in same HTML
- Test 4: Escapes values with special chars
- Test 5: Does not modify HTML structure (non-env content unchanged)
- Test 6: Returns empty string if env var missing

Use regex patterns from 01-RESEARCH.md under "Pattern 1, Stage 2: Post-Build HTML Rewriting".
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/html-expression-replacer.test.js --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - All 6 test cases pass (grep for "PASS" in output)
    - File `src/utils/html-expression-replacer.js` exports `replaceLeakedExpressions` function
    - Function signature: `(html, envVars) => html`
    - No script tags modified except env expression replacement (grep for "<script" count should match original)
    - Test assertions show no `import.meta.env` in resulting HTML (grep for "import.meta.env" should return 0)
  </acceptance_criteria>
  <done>html-expression-replacer.js and tests complete; all 6 tests green</done>
</task>

<task type="auto">
  <name>Task 3: Create integration test verifying end-to-end env injection</name>
  <files>
    src/utils/__tests__/env-injection-integration.test.js
  </files>
  <read_first>
    src/utils/env-preprocessor.js
    src/utils/html-expression-replacer.js
  </read_first>
  <action>
Create integration test `src/utils/__tests__/env-injection-integration.test.js` that:
1. Simulates real template files (mock Astro template with env vars)
2. Call preprocessAstroEnvVars with mock env object
3. Simulate a build (mock build output HTML)
4. Call replaceLeakedExpressions on mock HTML
5. Assert final output contains actual values, zero Astro expressions
6. Test realistic values: brand name, conversion IDs, tracking domain

Create 2-3 integration test cases:
- Case 1: Loan template (PUBLIC_BRAND, PUBLIC_CONVERSION_ID, PUBLIC_TRACKING_DOMAIN)
- Case 2: Template with missing env var (fallback behavior)
- Case 3: Template with special characters in values (quotes, apostrophes)

Each case: `files → preprocess → simulate build → replace leaked → verify no expressions`
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/env-injection-integration.test.js --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - All integration tests pass (grep "PASS")
    - Final HTML contains actual env values (grep for "LoanCorp" or test values should match)
    - No Astro expressions in final output (grep for "import.meta.env" returns 0)
    - Logs show both preprocessing and post-build stages executed (if logging added)
  </acceptance_criteria>
  <done>Integration test complete; 2-3 test cases green; demonstrates IMPORT-01 and IMPORT-02 working together</done>
</task>

</tasks>

<verification>
Run full test suite to ensure no regression:
```bash
npx vitest run src/utils/__tests__/env-preprocessor.test.js src/utils/__tests__/html-expression-replacer.test.js src/utils/__tests__/env-injection-integration.test.js --reporter=verbose
```

Expected: All tests green, no console errors, functions ready for Phase 2 build adapters to consume.
</verification>

<success_criteria>
- [ ] `env-preprocessor.js` exports `preprocessAstroEnvVars(files, envVars)`
- [ ] `html-expression-replacer.js` exports `replaceLeakedExpressions(html, envVars)`
- [ ] All 6 + 6 + 2-3 tests passing (16+ tests total)
- [ ] No Astro expression leaks in any test assertions
- [ ] Functions handle edge cases: missing vars, special chars, multiple expressions
- [ ] Requirements IMPORT-01 and IMPORT-02 verifiable via test assertions

**Blocked by:** Nothing (Wave 1, foundation)

**Blocks:**
- Plan 02 (capability detection — needs IMPORT-01/02 verified)
- Plan 03 (wizard integration — needs capability detection)
</success_criteria>

<output>
After completion, create `.planning/phases/01-template-import-capability/01-01-SUMMARY.md`
</output>
